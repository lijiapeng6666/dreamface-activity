import React from 'react';
import { Typography } from '@mui/material';

function UserPage(): React.ReactElement {
  return <Typography variant="h4">User Page</Typography>;
}

export default UserPage; 