import React from 'react';
import { Typography } from '@mui/material';

function BookingPage(): React.ReactElement {
  return <Typography variant="h4">Booking Page</Typography>;
}

export default BookingPage;
