import React from 'react';
import { Typography } from '@mui/material';

function BankingPage(): React.ReactElement {
  return <Typography variant="h4">Banking Page</Typography>;
}

export default BankingPage;
